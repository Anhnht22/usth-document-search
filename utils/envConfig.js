const envConfig = {
    endPoint: process.env.NEXT_PUBLIC_ENDPOINT_API,
    endPointStatic: process.env.NEXT_PUBLIC_ENDPOINT_STATIC,
    authToken: process.env.NEXT_PUBLIC_AUTH_TOKEN,
}

export default envConfig;